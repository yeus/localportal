{
  "devDependencies": {
    "globby": "^13.0.0",
    "marked": "^5.0.0",
    "pagecrypt": "^7.0.0"
  },
  "scripts": {
    "build:hugo": "hugo --minify",
    "encrypt": "bash scripts/encrypt.sh",
    "build": "yarn build:hugo && yarn encrypt",
    "start": "hugo server -D"
  }
}
